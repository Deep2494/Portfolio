
  # Create folder structure

  This is a code bundle for Create folder structure. The original project is available at https://www.figma.com/design/cb57kbI9sOVCuoszj8KgCL/Create-folder-structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  